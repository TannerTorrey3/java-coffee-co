import java.awt.*;

/**
 * Interface for all custom JComponents used within JavaCoffeeCo
 * Allows for access to constant fields and methods
 */
public interface CoffeeApp {
     /**
      *
      */
     int MAXIMUM_HEIGHT = 500;

     /**
      *
      */
     int MAXIMUM_WIDTH = 700;

     /**
      *
      */
     Color BACKGROUND_COLOR = new Color(150, 176, 255);

}
